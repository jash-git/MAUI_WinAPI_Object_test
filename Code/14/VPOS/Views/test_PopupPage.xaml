<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mct="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"             
             x:Class="VPOS.Views.test_PopupPage"
             CanBeDismissedByTappingOutsideOfPopup="False" Color="#0BE3FA">

    <VerticalStackLayout        
        BackgroundColor="Transparent"
        VerticalOptions="Center" 
        HorizontalOptions="Center">
        <Label
            x:Name="labtime"
            Text="test_PopupPage"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <ActivityIndicator Color="{StaticResource Primary}"
                               IsRunning="True" HeightRequest="100" WidthRequest="100"
                               IsVisible="True" />        
        <Button 
                x:Name="CloseBtn"
                Text="Close App" 
                Clicked="CloseBtn_Clicked"
                HorizontalOptions="Center" ClassId="" />        
    </VerticalStackLayout>
</mct:Popup>
